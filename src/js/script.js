const historyTime = document.querySelectorAll('history__time');
const historyShow = document.querySelectorAll('history__show');


historyTime.addEventListener('click', () => {
   menu.classList.toggle('history__show');
 });